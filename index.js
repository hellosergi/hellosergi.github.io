var parentPosX;
var parentPosY;

var parent = document.getElementById('asterisk');
var rectParent = parent.getBoundingClientRect();
// console.log(rectParent.top, rectParent.right, rectParent.bottom, rectParent.left);

var child = document.getElementById('curved');
var rectChild = curved.getBoundingClientRect();
// console.log(rectChild.top, rectChild.right, rectChild.bottom, rectChild.left);


// child.style.top = rectParent.top + 20 + "px"
// child.style.left = rectParent.left + 20 + "px";
